import 'package:flutter/material.dart';
class userAccount extends StatelessWidget {
  const userAccount({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('userAccount Page'),
      )
    );
  }
}